import SwiftUI

/// Placeholder for 0.0.5/0.0.10 permissions onboarding.
struct OnboardingView: View {
    var body: some View {
        Text("Onboarding comes in a later milestone")
            .padding()
    }
}
